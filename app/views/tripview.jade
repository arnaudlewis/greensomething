extends ./layout
block body
  h1 Travel
  div.content
    #trip-form
      script.
        console.log(ReactDOM)
        ReactDOM.render(
          React.createElement(UI.TripForm), document.getElementById('trip-form')
        )

